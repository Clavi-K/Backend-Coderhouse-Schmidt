extends layout/base.pug
block content

    if error
        p(style="color: red;")=error

    h1 Agregar un nuevo producto
    hr
    form(action="/pug/products", method="POST")

        label(for="name") Nombre del producto:
        input(type="text", name="name")

        label(for="price") Precio del producto:
        input(type="number", name="price")

        label(for="thumbnail") "Ruta" de imagen del producto:
        input(type="text", name="thumbnail")

        button(type="submit", class="btn btn-primary") Ingresar